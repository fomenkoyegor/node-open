const authController = require('./auth');
const postsController = require('./posts');
const commentsController = require('./commets');

module.exports = {
    authController,
    postsController,
    commentsController
};
